# 1. Search for any 'best.pt' file in your working directory
weight_files = list(Path('/kaggle/working').rglob('best.pt'))

if weight_files:
    # Use the most recent one found
    latest_weights = str(weight_files[-1])
    print(f"✅ Found weights at: {latest_weights}")
    
 # 2. Load the model using the discovered path
    model = YOLO(latest_weights)

 # 3. Run validation
    # Use the yaml_path variable we defined earlier
    metrics = model.val(data=yaml_path, split='test')
    print("✨ Validation successful!")
else:
    print("❌ Error: 'best.pt' not found. Did you finish the training cell?")
