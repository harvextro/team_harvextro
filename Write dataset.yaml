# ----Find dataset root -------
!find /kaggle/input/ -type d -name "train"

# ─── WRITE dataset.yaml ─────────────────────────────────────────
# This file is the "instruction manual" for the YOLOv8 trainer
yaml_cfg = {
    "path" : OUTPUT_ROOT,      # The root folder we created in Phase 2
    "train": "train/images",   # Training images path
    "val"  : "valid/images",   # Validation images path
    "test" : "test/images",    # Testing images path
    "nc"   : len(CLASSES),     # Number of classes (3)
    "names": CLASSES           # ["green_chili", "red_chili", "yellow_chili"]
}

yaml_path = f"{OUTPUT_ROOT}/dataset.yaml"

# Write the dictionary to a .yaml file
with open(yaml_path, "w") as f:
    yaml.dump(yaml_cfg, f, default_flow_style=False)

# 4.2 VERIFY THE CONFIGURATION
print(f"✅ dataset.yaml created successfully at: {yaml_path}\n")
print("--- [File Contents] ---")
print(open(yaml_path).read())
print("-----------------------")
