import os

# Search for the best.pt file automatically
for root, dirs, files in os.walk('/kaggle/working/'):
    for file in files:
        if file == "best.pt":
            print(f"✅ Found your model here: {os.path.join(root, file)}")
